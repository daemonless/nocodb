<!--
THIS FILE IS AUTOGENERATED - DO NOT EDIT MANUALLY
Source: dbuild templates
-->

# NocoDB

[![Build Status](https://img.shields.io/github/actions/workflow/status/daemonless/nocodb/build.yaml?style=flat-square&label=Build&color=green)](https://github.com/daemonless/nocodb/actions)
[![Last Commit](https://img.shields.io/github/last-commit/daemonless/nocodb?style=flat-square&label=Last+Commit&color=blue)](https://github.com/daemonless/nocodb/commits)

Free and self-hostable Airtable alternative — turn any database into a smart spreadsheet.

| | |
|---|---|
| **Port** | 8080 |
| **Registry** | `ghcr.io/daemonless/nocodb` |
| **Source** | [https://github.com/nocodb/nocodb](https://github.com/nocodb/nocodb) |
| **Website** | [https://nocodb.com/](https://nocodb.com/) |

## Version Tags
| Tag | Description | Best For |
| :--- | :--- | :--- |
| `latest` | **Upstream Binary**. Built from official release. | Most users — recommended. |

## Prerequisites
Before deploying, ensure your host environment is ready. See the [Quick Start Guide](https://daemonless.io/guides/quick-start) for host setup instructions.

## Deployment

### Podman Compose

```yaml
services:
  nocodb:
    image: "ghcr.io/daemonless/nocodb:latest"
    container_name: nocodb
    environment:
      - PUID=1000  # User ID for the application process
      - PGID=1000  # Group ID for the application process
      - TZ=UTC  # Timezone for the container
      - NC_DB=  # External database connection string (e.g. pg://host:5432?u=user&p=pass&d=db). Defaults to a local SQLite file in /config.
    volumes:
      - "/path/to/containers/nocodb:/config"
    ports:
      - "8080:8080"
    # always (not unless-stopped) so FreeBSD's podman rc.d auto-starts it at boot
    restart: always
```

Save as `compose.yaml`, then run `podman-compose up -d`.

### AppJail Director
**.env**:

```
# .env

DIRECTOR_PROJECT=nocodb
PUID=1000
PGID=1000
TZ=UTC
NC_DB=
```

**appjail-director.yml**:

```yaml
# appjail-director.yml

options:
  - virtualnet: ':<random> default'
  - nat:
services:
  nocodb:
    name: nocodb
    options:
      - container: 'boot args:--pull'
      - expose: '8080:8080 proto:tcp'
    oci:
      user: root
      environment:
        - PUID: !ENV '${PUID}'
        - PGID: !ENV '${PGID}'
        - TZ: !ENV '${TZ}'
        - NC_DB: !ENV '${NC_DB}'
    volumes:
      - nocodb: /config
volumes:
  nocodb:
    device: '/path/to/containers/nocodb'
```

**Makejail**:

```
# Makejail

ARG tag=latest

OPTION overwrite=force
OPTION from=ghcr.io/daemonless/nocodb:${tag}
```

Save the files above, then run `appjail-director up`.

**Note**: Exposing ports in AppJail means that your service can be reached from remote hosts. If that is not your intention, do not expose the ports and communicate with the service using the IPv4 address assigned by the virtual network.

### Podman CLI

```bash
podman run -d --name nocodb \
  -p 8080:8080 \
  -e PUID=1000 \
  -e PGID=1000 \
  -e TZ=UTC \
  -e NC_DB= \
  -v /path/to/containers/nocodb:/config \
  ghcr.io/daemonless/nocodb:latest
```

Save as `run.sh`, then run `sh run.sh`.

### AppJail

```bash
appjail oci run -Pd \
  -o overwrite=force \
  -o container="args:--pull" \
  -o virtualnet=":<random> default" \
  -o nat \
  -o expose="8080:8080 proto:tcp" \
  -e PUID=1000 \
  -e PGID=1000 \
  -e TZ=UTC \
  -e NC_DB= \
  -o fstab="/path/to/containers/nocodb /config <pseudofs>" \
  ghcr.io/daemonless/nocodb:latest nocodb
```

Save as `run.sh`, then run `sh run.sh`.

**Note**: Exposing ports in AppJail means that your service can be reached from remote hosts. If that is not your intention, do not expose the ports and communicate with the service using the IPv4 address assigned by the virtual network.

### Bastille

> [!WARNING]
> Bastille's OCI support is **experimental**. It requires `buildah`, shares the host network stack (`inherit`), and persists image-declared volumes under `--data-path`.

```yaml
services:
  nocodb:
    image: "ghcr.io/daemonless/nocodb:latest"
    container_name: nocodb
    network_mode: host  # jail shares host networking
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=UTC
      - NC_DB=
```

Save as `podman-compose.yml`, then run `bastille up`. Or via CLI:

```bash
bastille create -O \
  --env PUID=1000 \
  --env PGID=1000 \
  --env TZ=UTC \
  --env NC_DB= \
  --data-path /path/to/containers/nocodb \
  nocodb ghcr.io/daemonless/nocodb:latest inherit
```

### Ansible

```yaml
- name: Deploy nocodb
  containers.podman.podman_container:
    name: nocodb
    image: "ghcr.io/daemonless/nocodb:latest"
    state: started
    restart_policy: always
    env:
      PUID: "1000"
      PGID: "1000"
      TZ: "UTC"
      NC_DB: ""
    ports:
      - "8080:8080"
    volumes:
      - "/path/to/containers/nocodb:/config"
```

Save as `nocodb-deploy.yaml`, then run `ansible-playbook nocodb-deploy.yaml`.

Access at: `http://localhost:8080`

## Parameters

### Environment Variables

| Variable | Default | Description |
|----------|---------|-------------|
| `PUID` | `1000` | User ID for the application process |
| `PGID` | `1000` | Group ID for the application process |
| `TZ` | `UTC` | Timezone for the container |
| `NC_DB` | `` | External database connection string (e.g. pg://host:5432?u=user&p=pass&d=db). Defaults to a local SQLite file in /config. |

### Volumes

| Path | Description |
|------|-------------|
| `/config` | Configuration directory (SQLite database, uploaded attachments) |

### Ports

| Port | Protocol | Description |
|------|----------|-------------|
| `8080` | TCP | Web UI |

**Architectures:** amd64
**User:** `bsd` (UID/GID via PUID/PGID, defaults to 1000:1000)
**Base:** FreeBSD 15.1

---

Need help? Join our [Discord](https://discord.gg/c7GEYrvFtT) community.